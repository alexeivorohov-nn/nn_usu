name: nn_env
channels:
    - defaults
    - conda-forge
dependencies:
    - python=3.13
    - pip
    # - pytorch-cpu=2.8 # cpu-only
    - pytorch-gpu # for cuda
    - lightning
    - transformers
    - datasets
    - torchvision
    - numpy
    - pandas
    - scikit-learn
    - torchinfo
    - matplotlib
    - seaborn
    - jupyterlab
    - mlflow
    - optuna

